# Specs Directory

This directory stores task specs for `SPEC_REQUIRED` work.

Naming:

- `docs/specs/<task_id>.md` (example: `docs/specs/T-021.md`)

Template:

- Start from `docs/specs/T-000-TEMPLATE.md`

Rule:

- Spec must exist and be approved before implementation on `SPEC_REQUIRED` tasks.
